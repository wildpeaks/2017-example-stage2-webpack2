/* eslint-env mocha, browser */
import MyClass from '.';
import {strictEqual} from 'assert';


describe('MyClass', () => {
	it('Basic Tests', () => {
		const myobj = new MyClass();

		// Instance properties
		strictEqual(typeof myobj.instancePropertyBefore, 'string', 'instancePropertyBefore is a String');
		strictEqual(typeof myobj.instancePropertyAfter, 'string', 'instancePropertyAfter is a String');
		// strictEqual(typeof myobj.fakeProperty, 'string', 'fakeProperty is a String');

		// Instance methods
		// console.log('[boundArrow]', myobj.boundArrow('AAAAA '));
		// console.log('[boundFunction]', myobj.boundFunction('BBBBB '));

		// Static
		// console.log('[staticProperty]', MyClass.staticProperty);
		// console.log('[staticArrow]', MyClass.staticArrow('CCCCC '));
		// console.log('[staticFunction]', MyClass.staticFunction('DDDDD '));
	});
});
